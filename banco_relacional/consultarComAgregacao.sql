SELECT 
     regiao as 'Região',
     sum(populaçao) as Total
     from estados 
     group by regiao 
     order by Total desc


     SELECT 
    
     avg(populaçao) as Total
     from estados 
     




